#ifndef MAPREADER_H
#define MAPREADER_H
#include <SDL2/SDL.h>

// // Structure to hold map information
// typedef struct
// {
//     SDL_Texture *texture;
//     int width;
//     int height;
// } MapTile;

// int load_map(const char *json_file, SDL_Renderer *renderer, MapTile **map_tiles, int *tile_count);
// void render_map(SDL_Renderer *renderer, MapTile *map_tiles, int tile_count, int map_width, int map_height);

#endif
