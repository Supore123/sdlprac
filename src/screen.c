#include <time.h>
#include "screen.h"
#include "SDL2/SDL.h"

// vars
Window_t *mainWin = {NULL};

// screenInit -- Starts the screen initilisation process
appStatus_t screenInit(void)
{
    appStatus_t sc = APP_STATUS_OK;
    
    return sc;
}

// loadImage -- Loads an image to a screen
appStatus_t loadImage(void)
{
    appStatus_t sc = APP_STATUS_OK;

    return sc;
}